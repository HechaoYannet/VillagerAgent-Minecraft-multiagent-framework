# ===========================================
# VillagerAgent Docker 运行时依赖 (代码验证版)
# ===========================================
# 验证方法: grep "^import \|^from " over src/ model/ env/ pipeline/
# 最后验证: 2026-07-24
#
# 与根 requirements.txt 一致, 用于 Docker 构建层缓存优化。

# ── LLM 客户端 ──
openai>=1.30.0
google-generativeai>=0.3.0

# ── Web 框架 ──
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9
flask>=3.0.0
waitress>=3.0.0

# ── WebSocket ──
websockets>=12.0

# ── 配置 & 数据验证 ──
pydantic>=2.5.0
pyyaml>=6.0

# ── 日志 ──
colorlog>=6.8.0

# ── Token 计数 ──
tiktoken>=0.5.0

# ── Minecraft 桥接 ──
javascript>=1.0.0
names>=0.3.0

# ── 字符串匹配 ──
Levenshtein>=0.25.0
