# ===========================================
# VillagerAgent 运行时依赖
# ===========================================
# 与 pyproject.toml 的 [project] dependencies 保持一致。
# 生产部署推荐: pip install .  (使用 pyproject.toml)

# ── LLM 客户端 ──
openai>=1.30.0
httpx>=0.27.0

# ── Web 框架 ──
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9
flask>=3.0.0
waitress>=3.0.0

# ── WebSocket ──
websockets>=12.0

# ── 配置 & 数据验证 ──
pydantic>=2.5.0
pyyaml>=6.0

# ── 日志 ──
colorlog>=6.8.0

# ── Token 计数 ──
tiktoken>=0.5.0

# ── Minecraft 桥接 (JSPyBridge) ──
javascript>=1.0.0
names>=0.3.0

# ── 字符串匹配 (env/env_api.py) ──
python-Levenshtein>=0.25.0
